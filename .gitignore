.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
dist/
*.egg-info/

# Per-skill user data (gitignored for all skills — runtime state, config, notes).
# Conventions: each skill stores its data under skills/<name>/data/ and ships
# checked-in samples under skills/<name>/examples/.
skills/*/data/
.worktrees/
